import {INode} from '../nodes/node';

export interface IBonusList{
    bonusListId : number;
	node : Node ; 
	state : string ; 
	beginningDate : Date ;
	endDate : Date ;
	description :  string ; 
	    

}